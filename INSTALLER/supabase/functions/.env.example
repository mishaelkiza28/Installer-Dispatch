# Edge Function secrets — set these in Supabase → Edge Functions → Secrets
# (or `supabase secrets set --env-file supabase/functions/.env`). Never commit real values.
GMAIL_USER=tsgdispatch@gmail.com
GMAIL_APP_PASSWORD=abcd efgh ijkl mnop
# Optional display name on outgoing emails (defaults to "<Company> Dispatch")
EMAIL_FROM_NAME=TSG Solar Dispatch
